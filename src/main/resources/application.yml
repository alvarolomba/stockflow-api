spring:
  application:
    name: stockflow-api
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/stockflow}
    username: ${DATABASE_USERNAME:stockflow}
    password: ${DATABASE_PASSWORD:stockflow}
  jpa:
    defer-datasource-initialization: false
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  sql:
    init:
      mode: never

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    secret: ${JWT_SECRET:stockflow-demo-stable-jwt-secret-change-in-real-production}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:60}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}

server:
  port: ${PORT:8080}
  error:
    include-message: never
